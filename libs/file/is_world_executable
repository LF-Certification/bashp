# shellcheck shell=bash

# file::is_world_executable
# Checks if a file is world-executable.
#
# Description:
#   This function determines if the specified file has world-executable permissions. It uses the
#   `stat` command to retrieve the file's mode (permissions) and checks if the world-executable
#   bit is set.
#
# Usage:
#   file::is_world_executable <path>
#
# Parameters:
#   path - The path to the file to check.
#
# Returns:
#   Exits with status 0 (true) if the file is world-executable, otherwise exits with a non-zero
#   status (false).
function file::is_world_executable() {
    local path="$1"
    local mode
    mode=$(stat -c "%a" "$path")
    ((mode & 005))
}
